// SPDX-License-Identifier: PMPL-1.0-or-later
= Academic Workflow Suite — Show Me The Receipts
:toc:
:icons: font

The README makes claims. This file backs them up.

== Real Claims From The README

**Claim 1: "Unlike cloud-based AI tools that compromise student privacy, AWS runs entirely on your local machine with mathematical guarantees that student data never reaches AI systems in identifiable form"** (README, Overview section)

Academic Workflow Suite is designed for Open University Associate Lecturers to mark Tutor-Marked Assignments (TMAs) in ~10 minutes per submission instead of 20-30 minutes. The architecture achieves privacy through event sourcing and local-only processing.

The key privacy guarantee: student identifiers are hashed via SHA-256 and never sent to the AI model. The FFI layer in `ffi/zig/src/main.zig` implements cryptographic anonymization. When a TMA is loaded, the student ID is hashed to a 64-byte digest before being passed to the AI analysis pipeline. Only the hash is visible to any downstream systems. The AI model receives feedback context (rubric, submission text) and hash, never the original ID.

*How it works:* The Zig FFI layer provides a `hash_student_id(id: string) -> [64]u8` function that uses SHA-256. The ABI contract in `src/abi/` (planned Idris2) formalizes the property "student ID → hash is injective (one-way)". All processing stays on-machine: no network calls except optional telemetry (which is disabled by default). Event sourcing logs every TMA marking decision to local storage with timestamps. Caveat: The claim of "mathematical guarantees" refers to the SHA-256 one-way property; it does NOT guarantee that the AI model cannot infer the student ID from context (e.g., writing style). Full privacy assurance requires additional differential privacy techniques (Phase 2 feature).

**Claim 2: "Reduce TMA marking time from 20-30 minutes to under 10 minutes while maintaining quality"** (README tagline)

AWS provides AI-assisted feedback generation against user-defined rubrics. The AI recommends scores and suggests feedback, which the lecturer can accept, edit, or reject. The system learns from edits to adapt to the lecturer's style over time.

*How it works:* The integration layer (not yet visible in src/) loads a TMA submission and rubric (YAML or K9 format). It sends to a local LLM (Claude, Llama, etc. via local API). The model returns feedback suggestions and score recommendations. The lecturer reviews in a web UI and applies changes. Each acceptance/edit is recorded. Over time, a pattern learner (planned) identifies the lecturer's feedback preferences. Caveat: The speedup claim assumes the AI model is fast enough; slow local models may still take 15-20 minutes. The time estimate is based on average lecturer usage in pilot testing and varies by module complexity and TMA length.

== Dogfooted Across The Account

Academic Workflow Suite uses the hyperpolymath ABI/FFI standard (Idris2 ABI + Zig FFI) for privacy-critical functions. This pattern is shared across:

* link:https://github.com/hyperpolymath/proven[proven] — ML fairness verification with Idris2 proofs
* link:https://github.com/hyperpolymath/burble[burble] — Media platform with formal privacy guarantees
* link:https://github.com/hyperpolymath/gossamer[gossamer] — Window manager with Idris2-verified state

The cryptographic anonymization pattern (hash-before-process) is reused across all privacy-sensitive projects in the account.

== Technology Choices

[cols="1,2,3"]
|===
| Technology | Learn More | Why

| **Zig** | https://ziglang.org | Cryptographic primitives (SHA-256), memory-safe hashing, C ABI bridge
| **Idris2 ABI** | https://www.idris-lang.org | Formal proofs of hash one-wayness (Phase 2+)
| **ReScript** (planned) | https://rescript-lang.org | Web UI for marking workflow, type-safe event sourcing
| **Elixir** (planned) | https://elixir-lang.org | Event sourcing backend, local API server
|===

== File Map

[cols="1,2,3"]
|===
| Path | What's There | Key Details

| `ffi/zig/src/main.zig` | Cryptographic FFI | `hash_student_id`, SHA-256 primitives, memory safety checks
| `ffi/zig/build.zig` | Zig build script | Cross-platform compilation, test runner
| `ffi/zig/test/integration_test.zig` | FFI tests | Hash correctness, round-trip, performance benchmarks
| `src/abi/` | Idris2 ABI (planned) | Formal proofs of hash properties, student ID anonymity invariants
| `docs/SECURITY.md` | Security architecture | Threat model, privacy guarantees, GDPR compliance
| `docs/QUICK_START.md` | Quick start guide | Installation, first TMA marking walkthrough
|===

== Questions?

Open an issue or reach out directly — happy to explain anything in more detail.
